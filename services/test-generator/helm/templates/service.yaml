apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-generator.fullname" . }}
  labels:
    {{- include "test-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "test-generator.selectorLabels" . | nindent 4 }}

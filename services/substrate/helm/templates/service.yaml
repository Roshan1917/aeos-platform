apiVersion: v1
kind: Service
metadata:
  name: {{ include "substrate.fullname" . }}
  labels:
    {{- include "substrate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "substrate.selectorLabels" . | nindent 4 }}
